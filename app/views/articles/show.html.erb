<div class="container">
  <div class="my-5">
    <h1><%= @article.title %> <i class="fa-solid fa-bounce ms-3"><%= link_to "Update article", edit_article_path(@article.id), class:"btn btn-outline-primary"%></i> </h1>
  </div>
  <div class="card p-3">
    <p><%= @article.content %></p>
  </div>

</div>
